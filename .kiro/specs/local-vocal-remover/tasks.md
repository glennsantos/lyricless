# CLI verification tasks

- [ ] Run command-level checks for aliases, existing output, cleanup, formats, JSON lines, and exit codes.
- [ ] Run batch checks for duplicate basenames, unusual filenames, skip rule, counts, and failure status.
- [ ] Install into a fresh environment, run `pip check`, convert a short real file, and confirm the documented output decodes.
- [ ] Run `bash -n python/batch_process.sh python/run_vocal_remover.sh`.
